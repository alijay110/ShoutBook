spring:
  application:
    name: AuthenticationService
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:6603/shoutbook?useSSL=false
    username: admin
    password: abc123
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
     
server:
  port: 8181     

      